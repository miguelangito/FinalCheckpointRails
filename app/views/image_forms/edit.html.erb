<% content_for :title, "Editing image form" %>

<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-6">Editing image form</h1>

  <%= render "form", image_form: @image_form %>

  <div class="mt-6 flex justify-between">
    <%= link_to "Show this image form", @image_form, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    <%= link_to "Back to image forms", image_forms_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
</div>